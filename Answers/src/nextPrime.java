import java.util.Objects;
import java.util.Scanner;

public class nextPrime {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        int n = sc.nextInt();

        /*
        Loop through where i represents the next largest odd number, increases at n+2 (so i is only an odd), and i starts at the odd number after it.
        If the square root of i is an int then it is a composite number,
        if it is a float than it is a prime number.
         */
    }
}
